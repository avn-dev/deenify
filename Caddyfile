{
    auto_https off
}

https://localhost:8443, https://127.0.0.1:8443 {
    # Generate local certs (optional) and place them under ./certs
    # tls ./certs/localhost+2.pem ./certs/localhost+2-key.pem
    reverse_proxy 127.0.0.1:8000
}
